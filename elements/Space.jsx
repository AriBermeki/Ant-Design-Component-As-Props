import { Space } from 'antd';

const UISpace = ({ align, direction, size, split, wrap }) => {
  return (
    <Space align={align} direction={direction} size={size} split={split} wrap={wrap}>
      {/* Fügen Sie hier Ihre Kindkomponenten hinzu */}
    </Space>
  );
};

export default UISpace;
